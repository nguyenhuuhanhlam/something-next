import React, { Dispatch, createContext, useReducer } from 'react'

type StateType = {
	count: number
}

type ActionType = {
	type: string
}

export const CounterContextProvider = () => {
	return <h1>What?</h1>
}